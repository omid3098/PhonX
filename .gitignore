# Android build outputs
build/
app/build/

# Gradle caches and wrapper downloads (keep wrapper scripts)
.gradle/
**/gradle-wrapper.jar

# Local config — machine-specific, never commit
local.properties
*.iml

# IDE
.idea/
*.class

# OS
.DS_Store
Thumbs.db

# Go vendor dependencies (5k+ files, reproducible via `go mod vendor`)
phonxcore/vendor/

# Built AAR (binary, built from phonxcore/ Go module)
app/libs/phonxcore.aar

# Claude Code session state (task IDs, in-progress work)
.claude/
